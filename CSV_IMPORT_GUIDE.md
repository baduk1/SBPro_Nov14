# 📊 Руководство по импорту прайс-листа из CSV

## 🚀 Быстрый старт

### 1. Формат CSV файла

Ваш CSV файл должен содержать следующие колонки:

```csv
code,description,unit,rate,currency
E10/100,Walls,m2,44.80,GBP
F10/100,Floors,m2,33.70,GBP
E20/200,Columns,m3,248.00,GBP
L10/100,Door,piece,118.00,GBP
```

**Обязательные колонки:**
- `code` - уникальный код позиции (например, E10/100)
- `description` - описание позиции (например, "Walls")
- `unit` - единица измерения (например, m2, m3, piece)
- `rate` или `price` - цена за единицу (например, 44.80)

**Опциональные колонки:**
- `currency` - валюта (по умолчанию GBP)

### 2. Пример файла

Готовый пример находится в:
```
/root/skybuild_o1_production/backend/admin_assets/price_list.csv
```

Вы можете использовать его как шаблон для своих прайс-листов.

### 3. Как импортировать

1. Зайдите в раздел **Suppliers** (Поставщики)
2. Откройте нужного поставщика (например, "sup1")
3. Нажмите кнопку **"Import CSV"**
4. Выберите ваш CSV файл
5. Нажмите **"Import"**

Система покажет:
- Количество успешно импортированных позиций
- Количество пропущенных позиций
- Список ошибок (если есть)

### 4. Важные особенности

✅ **Обновление существующих позиций**
- Если позиция с таким `code` уже существует у данного поставщика, она будет обновлена новыми данными
- Если позиция новая - она будет добавлена

✅ **Поддержка обоих форматов**
- Система поддерживает как `rate`, так и `price` в качестве колонки с ценой
- Вы можете использовать любое из этих названий

✅ **Массовый импорт**
- Можно импортировать 1000+ позиций за один раз
- Процесс занимает несколько секунд

### 5. Типичные ошибки и решения

❌ **"Missing required fields"**
- Проверьте, что в CSV есть все обязательные колонки: code, description, unit, rate (или price)

❌ **"Invalid price format"**
- Убедитесь, что цена указана в числовом формате (например, 44.80, а не "£44.80")
- Используйте точку (.) в качестве разделителя десятичных, а не запятую

❌ **"Only CSV files are supported"**
- Убедитесь, что файл имеет расширение .csv
- Excel файлы (.xlsx) нужно сначала сохранить как CSV

### 6. Рекомендации

1. **Проверяйте данные перед импортом**
   - Откройте CSV файл в текстовом редакторе и проверьте формат
   - Убедитесь, что нет пустых строк в конце файла

2. **Используйте консистентные единицы измерения**
   - Всегда используйте одинаковые обозначения (например, всегда "m2", а не "кв.м")

3. **Не забывайте про валюту**
   - Если работаете с разными валютами, указывайте колонку `currency`

### 7. Пример: Создание CSV файла в Excel

1. Создайте таблицу с колонками: code, description, unit, rate, currency
2. Заполните данные
3. Нажмите **File → Save As**
4. Выберите формат **CSV UTF-8 (Comma delimited) (*.csv)**
5. Сохраните файл

### 8. Где получить помощь

- Проверьте логи бэкенда: `journalctl -u skybuild-backend -n 50 --no-pager`
- Пример файла: `/root/skybuild_o1_production/backend/admin_assets/price_list.csv`
- Документация проекта: `/root/skybuild_o1_production/PROJECT_HISTORY.md`

---

**Обновлено:** 2025-10-27

