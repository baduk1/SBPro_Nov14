# SkyBuild Pro - Инструкция по запуску для демо
## 16 октября 2025

---

## 📦 Структура проекта

```
skybuild_o1/
├── backend/              # FastAPI backend
├── apps/
│   ├── user-frontend/   # React app для пользователей (основной)
│   └── admin-frontend/  # React app для админов (опционально)
```

**Для демо нужно запустить:**
1. ✅ Backend (обязательно)
2. ✅ User Frontend (обязательно)
3. ❌ Admin Frontend (НЕ нужно для демо)

---

## 🚀 Пошаговый запуск

### Шаг 1: Миграции БД (выполнить ОДИН РАЗ!)

```bash
cd /Users/commander/Code_Projects/skybuild_o1/backend

# Активируй виртуальное окружение
source env/bin/activate
# ИЛИ если окружение называется .venv:
# source .venv/bin/activate

# Миграция 1: Регистрация + Email
python migrate_add_registration.py

# Миграция 2: Templates + Estimates
python migrate_add_templates_estimates.py
```

**Ожидаемый вывод:**
```
=== Starting Migration: Add Registration Support ===
✅ email_verified added
✅ credits_balance added
✅ full_name added
✅ email_verification_tokens table created!
=== Migration Completed Successfully! ===

=== Starting Migration: Add Templates & Estimates ===
✅ Tables created successfully!
=== Migration Completed Successfully! ===
```

---

### Шаг 2: Запуск Backend (обязательно)

**Терминал 1:**
```bash
cd /Users/commander/Code_Projects/skybuild_o1/backend

# Активируй окружение
source env/bin/activate  # или source .venv/bin/activate

# Запусти сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Проверка:**
- Backend: http://localhost:8000
- API Docs: http://localhost:8000/docs
- Должно быть: `INFO: Application startup complete.`

---

### Шаг 3: Запуск User Frontend (обязательно)

**Терминал 2 (новый терминал!):**
```bash
cd /Users/commander/Code_Projects/skybuild_o1/apps/user-frontend

# Установи зависимости (если первый раз)
npm install

# Запусти dev сервер
npm run dev
```

**Проверка:**
- Frontend: http://localhost:5173
- Должно быть: `➜ Local: http://localhost:5173/`

---

### Шаг 4: Admin Frontend (НЕ НУЖНО для демо!)

Admin Frontend **не нужен** для завтрашнего демо. Он используется только для:
- Управления access requests
- Редактирования global price lists
- Настройки DWG/IFC mappings

Всё это есть через user frontend или API.

**Если всё-таки нужно запустить:**
```bash
cd /Users/commander/Code_Projects/skybuild_o1/apps/admin-frontend
npm install
npm run dev
# Будет на порту 5174
```

---

## ✅ Проверка что всё работает

### 1. Backend работает?
Открой: http://localhost:8000/docs
Должна открыться Swagger документация API

### 2. User Frontend работает?
Открой: http://localhost:5173
Должна открыться landing page с градиентом

### 3. Всё вместе работает?
1. Зайди на http://localhost:5173
2. Нажми "Start Free Trial"
3. Зарегистрируйся
4. Должен увидеть "Check Your Email"

Если всё это работает - ты готов к демо! 🎉

---

## 🐛 Если что-то не работает

### Backend не запускается: "ModuleNotFoundError"
```bash
cd backend
source env/bin/activate
pip install -r requirements.txt
```

### Frontend не запускается: "Cannot find module"
```bash
cd apps/user-frontend
rm -rf node_modules package-lock.json
npm install
```

### Миграции не применились
```bash
cd backend
rm boq.db  # ОСТОРОЖНО: удаляет базу!
source env/bin/activate
python migrate_add_registration.py
python migrate_add_templates_estimates.py
```

### "Email already registered"
```bash
cd backend
sqlite3 boq.db "DELETE FROM users WHERE email='test@example.com';"
```

---

## 📝 Какие порты используются

| Сервис | Порт | URL | Нужен для демо? |
|--------|------|-----|-----------------|
| Backend | 8000 | http://localhost:8000 | ✅ ДА |
| User Frontend | 5173 | http://localhost:5173 | ✅ ДА |
| Admin Frontend | 5174 | http://localhost:5174 | ❌ НЕТ |

---

## 🎯 Быстрые команды (для повторного запуска)

**Backend:**
```bash
cd backend && source env/bin/activate && uvicorn app.main:app --reload
```

**User Frontend:**
```bash
cd apps/user-frontend && npm run dev
```

---

## 📂 README файлы (актуальны ли?)

### ✅ Backend README актуален
Файл: `backend/README.md`

**Что там:** Базовая инструкция запуска
**Проблемы:** ❌ Не упоминает миграции!

**Нужно добавить в backend/README.md:**
```markdown
## First time setup

Run migrations before starting the server:
```bash
python migrate_add_registration.py
python migrate_add_templates_estimates.py
```

### ✅ User Frontend README актуален
Файл: `apps/user-frontend/README.md`

**Что там:** Основные инструкции
**Проблемы:** ⚠️ Устаревшее описание (говорит про "Месяц 2")

**Можно обновить описание на:**
```markdown
# SkyBuild Pro — User Frontend (React + Vite + TypeScript)

Full-featured user interface for SkyBuild Pro:
- Landing page with registration
- User dashboard with credits tracking
- Suppliers management
- Templates & Estimates
- IFC/DWG file upload and processing
- BoQ generation and export (CSV/XLSX/PDF)
```

### ✅ Admin Frontend README актуален
Файл: `apps/admin-frontend/README.md`

**Что там:** Инструкции запуска
**Проблемы:** ✅ Всё ОК, но **не нужен для демо!**

---

## 💡 Резюме

### Для демо завтра запускай:

1. **Backend** (терминал 1)
2. **User Frontend** (терминал 2)

**Admin Frontend НЕ НУЖЕН!**

Всего **2 сервиса**, не 3!

---

## 🎉 Готово к запуску!

Следуй шагам выше и всё заработает. Удачи на демо! 🚀
